# Trello API credentials (server-side only — do NOT prefix with NEXT_PUBLIC_)
TRELLO_API_KEY=your-api-key
TRELLO_TOKEN=your-token
TRELLO_BOARD_ID=your-board-id          # From Trello URL: trello.com/b/{BOARD_ID}/...

# Team member Trello IDs (comma-separated)
# Find IDs: GET https://api.trello.com/1/boards/{boardId}/members?key=...&token=...
TEAM_MEMBER_IDS=daniel-member-id,nathan-member-id

# Members to exclude from workload views (e.g., boss)
EXCLUDE_MEMBER_IDS=randall-member-id
